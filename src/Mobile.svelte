<script>
  // Guides are desktop-only for now: steps target record-form field classes,
  // and the mobile form doesn't lay fields out the same way. This is the hook
  // to build on when mobile support lands.
  let { config } = $props();
</script>
